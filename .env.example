# Copy to .env and adjust as needed. .env is gitignored.

# Server
PORT=8080

# Postgres (matches docker-compose.yml). Used from Week 2 onward.
DATABASE_URL=postgres://whiteboard:whiteboard@localhost:5432/whiteboard
