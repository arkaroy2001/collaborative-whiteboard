// Placeholder app shell. The Week 1 canvas + WebSocket client goes here next,
// built against the @whiteboard/shared wire protocol.

export function App() {
  return <h1>Whiteboard — scaffolded</h1>;
}
